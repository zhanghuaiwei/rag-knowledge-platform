<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ragkb.service.modules.knowledge.persistence.mapper.IndexProfileMapper">

    <!--
        index_profile 表自定义查询骨架。
        单表 CRUD 由 BaseMapper 提供，无需在此声明；
        JOIN / 聚合等自定义 SQL（resultMap + select 等）由业务实现时在此编写。
    -->

</mapper>
