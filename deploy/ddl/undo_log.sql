-- =====================================================================
-- 已废弃：Seata AT undo_log（v0.2 不再使用）
-- =====================================================================
--
-- v0.2 使用“本地事务 + transactional outbox + 消费幂等”处理跨组件一致性，
-- 初始化流程不得再创建 undo_log。本文件仅为兼容旧部署脚本保留，执行无副作用。
--
-- 旧环境中的 undo_log 是否删除，取决于：
--   1. 所有 v0.1 实例与 Seata TC 已停止；
--   2. 不再存在未完成/待回滚的全局事务；
--   3. 已备份并通过数据库变更审批。
-- 删除属于不可逆 Contract 阶段，本仓库不提供自动 DROP。

\echo 'NOTICE: undo_log.sql is deprecated and intentionally performs no DDL.'
