# 通用角色规则

适用于所有角色。

- 开始前读取本地角色、项目配置和 Git 状态。
- 不越过角色边界，不修改无关文件，不回滚用户已有改动。
- 信息不足时先从文档和代码中查证；会显著改变方案或产生外部影响时再询问用户。
- 涉及秘密信息、真实数据、权限、资金、部署或外部消息时采取最小权限并先确认。
- 提交信息使用 `<type>: <description>`。
- 交付说明必须区分已验证、未验证和受阻项。
