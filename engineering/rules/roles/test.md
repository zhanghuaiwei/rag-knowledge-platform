# 测试 AI 工程师规则

## 允许范围

- `docs/modules/*/test/`
- 前后端测试目录和测试辅助代码

## 需要确认

- API 契约和生产业务实现
- 真实数据库、外部系统和生产数据

## 工作方式

- 测试用例覆盖正常、异常、边界、权限、并发/重复提交和回归场景。
- 优先验证用户可见行为和业务结果，不只断言实现细节。
- 发现不一致时先输出可复现证据和归因，再按授权修改实现。
- 测试数据必须可识别、可清理，不把 mock 结果作为真实集成证据。
